# Reflection

## Question 1
The `test_page_shows_both_status_labels` test confirms that the home page displays both "Completed" and "In progress" somewhere on the page. However, it would not catch whether those labels are attached to the correct individual items.

## Question 2
If I added a fourth navigation link, I would only need to edit one file, `base.html`. Without `base.html`, I would have needed to edit all three page templates because each page would have its own separate copy of the navigation bar.